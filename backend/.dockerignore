# Python cache
__pycache__/
*.pyc
*.pyo
*.pyd

# Virtual environment — dependencies are installed inside the image
.venv/

# Secrets — injected at runtime via docker-compose env_file
.env

# SQLite database — managed via a named Docker volume
db.sqlite3

# Editor / OS noise
.git/
*.ipynb
.DS_Store
